import Layout from "@/app/layout";
import SearchView from "@/views/SearchView";

const SearchPage = () => {
  return (
    <Layout headerData={[]}>
      <SearchView />
    </Layout>
  );
};

export default SearchPage;
