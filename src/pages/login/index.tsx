import Layout from "@/app/layout";
import LoginView from "@/views/Login";

const LoginPage = () => {
  return (
    <Layout headerData={[]}>
      <LoginView />
    </Layout>
  );
};

export default LoginPage;
