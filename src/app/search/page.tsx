import Layout from "@/layout/layout";
import SearchView from "@/views/SearchView";

const SearchPage = () => {
  return (
    <Layout>
      <SearchView />
    </Layout>
  );
};

export default SearchPage;
